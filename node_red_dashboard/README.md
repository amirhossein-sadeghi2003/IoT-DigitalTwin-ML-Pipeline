# Node-RED Dashboard

## Import
1) Open Node-RED editor
2) Menu → Import → select `flow.json`
3) Deploy

## MQTT
- Input topic: `iot/model/input`
- Predictions topic: `iot/model/predictions`

## Notes
If you changed MQTT host/port in Node-RED, make sure it matches your broker (default: `localhost:1883`).
